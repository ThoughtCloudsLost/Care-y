diff --git a/shared/calc-popover-position.js b/shared/calc-popover-position.js
index 3163bcf68efefdbe6d177e52d668bff06bcd81c8..3ee50168c8ccf5319ddc9f71f92ed1a76590acbe 100644
--- a/shared/calc-popover-position.js
+++ b/shared/calc-popover-position.js
@@ -7,7 +7,8 @@ export const calcPopoverPosition = ({
   targetY,
   targetWidth = 0,
   targetHeight = 0,
-  theme
+  theme,
+  placement
 }) => {
   if (targetEl.current) {
     targetEl = targetEl.current;
@@ -66,11 +67,18 @@ export const calcPopoverPosition = ({
 
   // Top Position
   let position = 'top';
-  if (popoverHeight + angleSize < targetOffsetTop - safeAreaTop) {
-    // On top
+  if (placement === 'bottom') {
+    // Forced bottom placement
+    position = 'bottom';
+    top = targetOffsetTop + targetHeight + angleSize;
+  } else if (placement === 'top') {
+    // Forced top placement
+    top = targetOffsetTop - popoverHeight - angleSize;
+  } else if (popoverHeight + angleSize < targetOffsetTop - safeAreaTop) {
+    // On top (auto)
     top = targetOffsetTop - popoverHeight - angleSize;
   } else if (popoverHeight + angleSize < appHeight - targetOffsetTop - targetHeight) {
-    // On bottom
+    // On bottom (auto)
     position = 'bottom';
     top = targetOffsetTop + targetHeight + angleSize;
   } else {
diff --git a/shared/classes/MessagebarClasses.js b/shared/classes/MessagebarClasses.js
index 0143714c3d284cda7aac1204d33262948cd074aa..ad35cdf2b80fb85fec710b410c532fe38b67de27 100644
--- a/shared/classes/MessagebarClasses.js
+++ b/shared/classes/MessagebarClasses.js
@@ -19,12 +19,12 @@ export const MessagebarClasses = (props, colors, {
       ios: 'w-full items-center gap-3'
     },
     left: {
-      common: cls('', leftClassName || leftClass),
+      common: cls('flex items-center', leftClassName || leftClass),
       ios: cls('', colors.toolbarIconIos),
       material: cls('mt-1 -ms-2', colors.toolbarIconMd)
     },
     right: {
-      common: cls('', rightClassName || rightClass),
+      common: cls('flex items-center', rightClassName || rightClass),
       ios: cls('', colors.toolbarIconIos),
       material: cls('mt-1 -me-2')
     },
diff --git a/shared/classes/NavbarClasses.js b/shared/classes/NavbarClasses.js
index 6fbf74ceeb19fcdc938f7b5ea570942d0cc42970..5d3f2d03fc5f900e62aba8ec921029d101fff7a2 100644
--- a/shared/classes/NavbarClasses.js
+++ b/shared/classes/NavbarClasses.js
@@ -39,11 +39,11 @@ export const NavbarClasses = (props, colors, baseClassName) => {
       material: cls('pt-safe', fontSizeMaterial, colors.textMaterial)
     },
     bgBlur: {
-      ios: cls('backdrop-blur-[2px] absolute left-0 top-0 w-full  mask-b-to-100% pointer-events-none mask-b-from-50%', subnavbar ? 'h-[calc(max(16px,var(--k-safe-area-top))_+_44px_+70px_+_16px)]' : 'h-[calc(max(16px,var(--k-safe-area-top))_+_44px_+_16px)]')
+      ios: cls('backdrop-blur-[2px] absolute left-0 top-0 w-full  mask-b-to-100% pointer-events-none mask-b-from-50%', subnavbar ? 'h-[calc(max(16px,var(--k-safe-area-top))_+_44px_+70px_+_16px)]' : 'h-[var(--k-navbar-chrome-h,calc(max(16px,var(--k-safe-area-top))_+_44px_+_16px))]')
     },
     bg: {
       common: cls('absolute w-full left-0 top-0', bgClassName || bgClass),
-      ios: cls(colors.bgIos, 'pointer-events-none', subnavbar ? 'h-[calc(max(16px,var(--k-safe-area-top))_+_44px_+70px_+_16px)]' : 'h-[calc(max(16px,var(--k-safe-area-top))_+_44px_+_16px)]'),
+      ios: cls(colors.bgIos, 'pointer-events-none', subnavbar ? 'h-[calc(max(16px,var(--k-safe-area-top))_+_44px_+70px_+_16px)]' : 'h-[var(--k-navbar-chrome-h,calc(max(16px,var(--k-safe-area-top))_+_44px_+_16px))]'),
       material: cls('h-full', colors.bgMaterial, outline && 'hairline-b')
     },
     subnavbar: {
diff --git a/shared/colors/MessagebarColors.js b/shared/colors/MessagebarColors.js
index 1bb74e299d2dc5cac99299e4b90252207b60a483..1d761000429c47ffa753cbbf1d3729aab4ade158 100644
--- a/shared/colors/MessagebarColors.js
+++ b/shared/colors/MessagebarColors.js
@@ -8,8 +8,8 @@ export const MessagebarColors = (colorsProp = {}, dark) => {
     inputBgMd: cls('bg-md-light-surface-2', dark('dark:bg-md-dark-surface-variant')),
     placeholderIos: cls(dark('dark:placeholder-whit/40')),
     placeholderMd: cls('placeholder-md-light-on-surface-variant', dark('dark:placeholder-md-dark-on-surface-variant')),
-    bgIos: undefined,
-    bgMaterial: cls('bg-md-light-surface', dark('dark:bg-md-dark-surface')),
+    bgIos: cls('bg-gradient-to-t from-ios-light-surface to-transparent', dark('dark:from-ios-dark-surface/50')),
+    bgMaterial: cls('bg-md-light-surface-2', dark('dark:bg-md-dark-surface-2')),
     ...filterColors(colorsProp)
   };
 };
\ No newline at end of file
diff --git a/svelte/components/Dialog.svelte b/svelte/components/Dialog.svelte
index f83845881302040a240af0d836464932551573e6..711eaddc598a715800f44f46cd4752262a70084f 100644
--- a/svelte/components/Dialog.svelte
+++ b/svelte/components/Dialog.svelte
@@ -3,6 +3,7 @@
   import { DialogColors } from '../../shared/colors/DialogColors.js';
   import { useDarkClasses } from '../shared/use-dark-classes.js';
   import { useThemeClasses } from '../shared/use-theme-classes.js';
+  import { printText } from '../shared/print-text.js';
   import Glass from './Glass.svelte';
 
   let {
diff --git a/svelte/components/Link.svelte b/svelte/components/Link.svelte
index 5b219dc6403e587a20f5b2ce79fcc1cbc152db3f..04b079572a40038fcc746f75487dc5c9839a9689 100644
--- a/svelte/components/Link.svelte
+++ b/svelte/components/Link.svelte
@@ -119,15 +119,27 @@
   });
 </script>
 
+<!-- WAI-ARIA: keyboard activation for non-native interactive elements.
+     Enter activates all interactive roles. Space activates buttons/tabs
+     but NOT links (WAI-ARIA Link pattern). -->
 <svelte:element
   this={component}
   bind:this={el}
   class={classes}
-  {...restProps}
-  {...linkProps}
   role="link"
   tabindex="0"
+  {...restProps}
+  {...linkProps}
   onclick={onClick || onclick}
+  onkeydown={(e) => {
+    restProps.onkeydown?.(e);
+    if (e.defaultPrevented) return;
+    const role = e.currentTarget?.getAttribute('role');
+    if (e.key === 'Enter' || (e.key === ' ' && role !== 'link')) {
+      e.preventDefault();
+      e.currentTarget?.click();
+    }
+  }}
 >
   {@render children?.()}
 </svelte:element>
diff --git a/svelte/components/Messagebar.svelte b/svelte/components/Messagebar.svelte
index c2fbdb2c614369629d1cdff35bbccee3d38ee173..20a4ae45e250d4142cd2087feb592ba39d1cd9bb 100644
--- a/svelte/components/Messagebar.svelte
+++ b/svelte/components/Messagebar.svelte
@@ -5,6 +5,7 @@
   import { useDarkClasses } from '../shared/use-dark-classes.js';
   import Toolbar from './Toolbar.svelte';
   import Glass from './Glass.svelte';
+  import ToolbarPane from './ToolbarPane.svelte';
 
   let {
     class: className,
@@ -92,9 +93,9 @@
 >
   <Toolbar {colors} {outline} innerClass={c.toolbarInner}>
     {#if left}
-      <div class={c.left}>
+      <ToolbarPane tabbar={false} class={c.left}>
         {@render left?.()}
-      </div>
+      </ToolbarPane>
     {/if}
     <Glass class={c.messagebarArea}>
       <textarea
@@ -113,9 +114,9 @@
       ></textarea>
     </Glass>
     {#if right}
-      <div class={c.right}>
+      <ToolbarPane tabbar={false} class={c.right}>
         {@render right?.()}
-      </div>
+      </ToolbarPane>
     {/if}
   </Toolbar>
 </svelte:element>
diff --git a/svelte/components/Popover.svelte b/svelte/components/Popover.svelte
index 25ebd4b7069a8af1028228eb8451ba0678bd49b2..ef71ff2eb90df7fa282da5579757f363d7b54ba9 100644
--- a/svelte/components/Popover.svelte
+++ b/svelte/components/Popover.svelte
@@ -28,6 +28,7 @@
     targetY = undefined,
     targetWidth = undefined,
     targetHeight = undefined,
+    placement = undefined,
 
     children,
     ...restProps
@@ -76,6 +77,7 @@
       targetWidth,
       targetHeight,
       theme,
+      placement,
     });
   };
 
diff --git a/svelte/components/Searchbar.svelte b/svelte/components/Searchbar.svelte
index 317608318ac2f0234bfb46b5b5c7a2954917505c..82d95924ee83cc1e8a06c26a74d0382b0b6ef56a 100644
--- a/svelte/components/Searchbar.svelte
+++ b/svelte/components/Searchbar.svelte
@@ -46,7 +46,7 @@
     ...restProps
   } = $props();
 
-  let theme = $state(useTheme({ ios, material }));
+  let theme = $derived(useTheme({ ios, material }));
   let disableTimeout = null;
 
   let el = $state(null);
diff --git a/svelte/components/Stepper.svelte b/svelte/components/Stepper.svelte
index 019e8f3c47c0a8b8ed78c98964e32e3ba8c8ca43..095479b639a380b05896f3df65dad9323b1fe3be 100644
--- a/svelte/components/Stepper.svelte
+++ b/svelte/components/Stepper.svelte
@@ -62,7 +62,7 @@
   let el = $state(null);
   export { el };
 
-  let theme = $state(useTheme({ ios, material }));
+  let theme = $derived(useTheme({ ios, material }));
 
   const isRounded = $derived(
     typeof rounded === 'undefined'
diff --git a/svelte/components/Toggle.svelte b/svelte/components/Toggle.svelte
index 6fb59773a4d9ca8e3d98441cb4b47ca1807dad64..4b925ca71182f137abdf524373bff4c59a55b4de 100644
--- a/svelte/components/Toggle.svelte
+++ b/svelte/components/Toggle.svelte
@@ -70,7 +70,7 @@
     <span class={c.thumbSide[state]}></span>
     <span class={c.thumbBg[state]}></span>
     <span class={c.thumbShadow[state]}></span>
-    <span ref={rippleTargetElRef} class={c.thumbWrap[state]}>
+    <span bind:this={rippleTargetElRef} class={c.thumbWrap[state]}>
       <span class={c.thumb[state]}></span>
     </span>{:else}
     <span bind:this={rippleTargetElRef} class={c.thumbWrap[state]}>
diff --git a/svelte/components/ToolbarPane.svelte b/svelte/components/ToolbarPane.svelte
index cb4657f834c2499dcff4ddebddfdf620cc3ea614..2b27a3805bcefbe06768d28a3f9ce358bc25b463 100644
--- a/svelte/components/ToolbarPane.svelte
+++ b/svelte/components/ToolbarPane.svelte
@@ -13,6 +13,7 @@
     class: className,
     ios = undefined,
     material = undefined,
+    tabbar: tabbarProp = undefined,
     colors: colorsProp,
     children,
     ...restProps
@@ -45,7 +46,7 @@
       },
     }));
 
-  const { tabbar, activeTabbarEl } = $derived(
+  const { tabbar: ctxTabbar, activeTabbarEl } = $derived(
     ToolbarContext()?.value || {
       toolbar: false,
       tabbar: false,
@@ -54,6 +55,7 @@
       activeTabbarEl: undefined,
     }
   );
+  const tabbar = $derived(tabbarProp !== undefined ? tabbarProp : ctxTabbar);
   const hasHighlight = $derived(theme === 'ios' && tabbar);
 
   const dark = useDarkClasses();
diff --git a/svelte/types/Popover.d.ts b/svelte/types/Popover.d.ts
index 126bebc04eff64fb1745417e0ea6d9e8167e8e5b..bd07ea417df130ea17e795d5e8c646f2bed4c5cb 100644
--- a/svelte/types/Popover.d.ts
+++ b/svelte/types/Popover.d.ts
@@ -74,6 +74,12 @@ export interface Props {
    * Virtual target element height (in px). Required without using real target element (`target` prop)
    */
   targetHeight?: number;
+  /**
+   * Force popover vertical placement. Skips auto-detection.
+   *
+   * @default undefined (auto)
+   */
+  placement?: 'top' | 'bottom';
 }
 
 
diff --git a/svelte/types/ToolbarPane.d.ts b/svelte/types/ToolbarPane.d.ts
index 584f9ac73f7107816319c1784e36390e23e00697..0c792b17fcbac9f4eabdb3f1a23d0199f974d9c0 100644
--- a/svelte/types/ToolbarPane.d.ts
+++ b/svelte/types/ToolbarPane.d.ts
@@ -9,6 +9,13 @@ export interface Props {
    * @default 'div'
    */
   component?: string | typeof SvelteComponent;
+  /**
+   * Override the parent Toolbar's tabbar context for this pane.
+   * Set to false to disable the iOS highlight and w-full class.
+   *
+   * @default undefined (inherits from parent Toolbar context)
+   */
+  tabbar?: boolean;
   /**
    * Object with Tailwind CSS colors classes
    * */
