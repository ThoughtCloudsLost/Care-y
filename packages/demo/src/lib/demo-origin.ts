/**
 * Fake origin used by the demo router and app-state stub. A demo URL
 * needs a valid origin for the URL constructor but never hits the
 * network; this constant keeps it in one place.
 */
export const DEMO_ORIGIN = "http://demo.local";
