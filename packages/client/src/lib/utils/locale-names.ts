/**
 * Native-language display names for the user-facing locales.
 * Locales absent from this map (dev pseudolocales like en-XA) are not
 * offered to users.
 */
export const NATIVE_LOCALE_NAMES: ReadonlyMap<string, string> = new Map([
  ["en", "English"],
  ["es", "Español"],
]);
